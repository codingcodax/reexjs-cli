import { WriteStyleScripting } from '../../types';

const writeStyleScripting: WriteStyleScripting = (
  styleScripting,
  framework,
  appDirectory
) => {
  console.log(styleScripting, framework, appDirectory);
};

export default writeStyleScripting;
