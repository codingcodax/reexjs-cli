import { StyleScripting, Framework, AppDirectory } from '../';

type WriteStyleScripting = (
  styleScripting: StyleScripting,
  framework: Framework,
  appDirectory: AppDirectory
) => void;

export default WriteStyleScripting;
